---
- name: Verify
  hosts: all
  vars_files:
    - ../../defaults/main.yml
  tasks:
    - name: verify cosign
      include: verify_cosign.yml
    - name: verify dive
      include: verify_dive.yml
    - name: verify eksctl
      include: verify_eksctl.yml
    - name: verify helm
      include: verify_helm.yml
    - name: verify k9s
      include: verify_k9s.yml
    - name: verify kapp
      include: verify_kapp.yml
    - name: verify kp
      include: verify_kp.yml
    - name: verify kubectl
      include: verify_kubectl.yml
    - name: verify kubeseal
      include: verify_kubeseal.yml
    - name: verify pack
      include: verify_pack.yml
    - name: verify skopeo
      include: verify_skopeo.yml
      when: (ansible_os_family == 'RedHat' and ansible_distribution != 'Amazon') or
            (ansible_distribution == 'Ubuntu' and ansible_distribution_major_version|int >= 22)
    - name: verify ytt
      include: verify_ytt.yml
